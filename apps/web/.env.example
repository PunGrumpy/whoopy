# WHOOP Developer App credentials (https://developer.whoop.com)
WHOOP_CLIENT_ID=""
WHOOP_CLIENT_SECRET=""

# Session secret for JWT cookie encryption (32+ random characters)
# Generate: openssl rand -base64 32
SESSION_SECRET=""

# Optional: override auto-derived OAuth redirect URI
# Defaults to http://localhost:3000/api/auth/callback locally
# or https://<VERCEL_URL>/api/auth/callback on Vercel
# WHOOP_REDIRECT_URI="https://your-domain.com/api/auth/callback"
